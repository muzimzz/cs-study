## JPA

<details>
    <summary><b>JDBC, Spring JDBC</b></summary>
    JDBC란 자바 애플리케이션에서 데이터베이스에 접근하기 위한 API(인터페이스)이다.  
    Spring JDBC는 DB연결, 예외처리, 자원반납 등의 작업을 스프링이 대신 처리해줌으로써  
    개발자는 쿼리 작성, 결과 매핑에만 집중할 수 있다. 실제 DB와의 통신은 각 DB의 JDBC Driver가 담당한다. 
</details>

<details>
    <summary><b>DataSource</b></summary>
</details>

---

<details>
    <summary><b>Sql Mapper(MyBatis)</b></summary>
    sql을 자바코드 안이 아니라 xml에 따로 작성하여 분리하며, RowMapper를 직접 작성하는 대신  
    ResultType을 지정하여 자동으로 매핑한다.  
    `<if>`나 `<foreach>` 등 동적 쿼리를 작성할 수 있는 xml문법도 제공한다.
</details>

---

<details>
    <summary><b>JPA, Hibernate</b></summary>
    JPA는 개발자가 sql을 직접 작성하지 않고, JpaRepository 인터페이스에 구현된 메서드를 호출하면  
    Hibernate가 자동으로 테이블을 생성하고 sql을 작성하고 결과를 엔티티 클래스에 매핑한다.  
    복잡하거나 동적인 쿼리는 QueryDSL을 사용해 작성하기도 한다.  
    Hibernate는 JPA의 기본 구현체로, 위에서 언급한 기능 말고도  
    트랜잭션 연동, 영속성 컨텍스트 관리 등의 역할을 한다.  
</details>

<details>
    <summary><b>ORM</b></summary>
    Hibernate와 같은 JPA의 구현체가 ORM을 사용하여 자바의 객체 중심 설계와  
    데이터베이스의 관계형 설계 사이의 불일치를 해결한다.  
    개발자는 엔티티를 객체로써 다루면  ORM이 연관관계 처리를 대신 수행한다.
</details>

<details>
    <summary><b>영속성 컨텍스트</b></summary>
    영속성 컨텍스트는 자바 애플리케이션과 DB사이에서 엔티티를 관리하는 저장 공간으로,  
    데이터를 가져올 때 1차캐시에 값이 없다면 DB에서 조회한 후 엔티티를 영속성 컨텍스트에 저장하고  
    이후 같은 트랜잭션 내에서 동일한 엔티티를 조회하면 캐시에서 바로 반환한다.  
    1차캐시는 트랜잭션 범위 내에서만 유지되며 다른 트랜잭션과 공유되지 않는다.   
    엔티티를 저장할 때는 쓰기 지연 방식으로 DB에 바로 저장하지 않고 트랜잭션 커밋 시점에 SQL을 한 번에 실행한다.  
    또한 영속상태인 엔티티가 변경되었을 때 역시 1차 캐시에만 반영해 두었다가 트랜잭션 커밋 시점에
    처음에 조회할 때 저장해둔 스냅샷과 현재 상태를 비교하여 변경이 감지되면 update쿼리를 실행한다.  
</details>

<details>
    <summary><b>OSIV</b></summary>
</details>

---